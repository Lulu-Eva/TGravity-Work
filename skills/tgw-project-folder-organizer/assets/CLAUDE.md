# Claude Code 规则

本项目规则以 `AGENTS.md` 和 `SOURCE_OF_TRUTH.md` 为准。

处理本目录前：

1. 先读 `SOURCE_OF_TRUTH.md`。
2. 再读 `AGENTS.md`。
3. 不绕过敏感目录、迁移清单和用户确认规则。

如果 `CLAUDE.md` 与 `AGENTS.md` 冲突，以 `AGENTS.md` 为准，并报告冲突。
